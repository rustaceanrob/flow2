export default interface SettingsConfig {
    has_configured: boolean,
    integrated_tor: boolean,
    public_server: boolean,
    wrapped_segwit: boolean,
    rpc_username: string,
    rpc_password: string,
}